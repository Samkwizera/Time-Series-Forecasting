# Data (raw files are 20.8 GB; interim/processed are regenerated by scripts/01_ingest.py)
data/*
!data/README.md

# Trained models and checkpoints (root-level only; src/milan_forecast/models is source code)
/models/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# LaTeX build artefacts (the built PDF is kept)
report/*.aux
report/*.bbl
report/*.blg
report/*.log
report/*.out
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz

# Video recordings. The presentation is submitted and linked as a URL, never committed:
# the files are large and GitHub is not the place for them. video/outline.md is kept.
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.m4v

# Smoke-test outputs
reports/smoke/
experiments/smoke/

# OS
.DS_Store

# Report artefacts. These are regenerated by scripts/07_report_assets.py + report/build.sh,
# but they are committed so that report.tex compiles from a fresh clone without first
# re-running the full 20.8 GB pipeline (the macros in generated/numbers.tex hold every
# number quoted in the text).
